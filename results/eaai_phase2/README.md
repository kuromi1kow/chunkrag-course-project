# EAAI Phase 2 results

Full runs write private, benchmark-derived question-level results below
`results/eaai_phase2/techqa_adaptive_v1/`. These files are ignored by Git and
must not be placed in an anonymous submission package without a separate data
sharing review.

The directory is intentionally separate from every frozen canonical and prior
submission artifact.
