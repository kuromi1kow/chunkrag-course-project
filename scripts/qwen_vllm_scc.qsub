#!/bin/bash -l
#$ -j y

set -euo pipefail

REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
OUTPUT_DIR="${1:-$REPO_ROOT/outputs/openwebui_vllm}"

cd "$REPO_ROOT"
bash "$REPO_ROOT/scripts/run_scc_vllm_server.sh" "$OUTPUT_DIR"
